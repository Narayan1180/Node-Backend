<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>

  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #e3f2fd, #f9f9ff);
    }

    .card {
      width: 100%;
      max-width: 380px;
      background: #fff;
      padding: 30px;
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.12);
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #222;
    }

    label {
      font-size: 14px;
      color: #555;
    }

    input {
      width: 100%;
      padding: 12px;
      margin: 8px 0 18px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 15px;
    }

    input:focus {
      outline: none;
      border-color: #4A90E2;
      box-shadow: 0 0 0 2px rgba(74,144,226,0.15);
    }

    button {
      width: 100%;
      background: #4A90E2;
      color: white;
      padding: 12px;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background: #3b7fd8;
    }

    .links {
      text-align: center;
      margin-top: 18px;
      font-size: 14px;
    }

    .links a {
      color: #4A90E2;
      text-decoration: none;
      margin: 0 6px;
    }

    /* Flash messages */
    .flash-msg {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      padding: 12px 22px;
      border-radius: 8px;
      font-size: 14px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.12);
      z-index: 999;
      animation: fadeOut 4s forwards;
    }

    .success {
      background: #dcfce7;
      color: #166534;
    }

    .error {
      background: #fee2e2;
      color: #991b1b;
    }

    @keyframes fadeOut {
      0% { opacity: 1; }
      80% { opacity: 1; }
      100% { opacity: 0; }
    }

    
/* Full page center */
.page-center {
  width: 100%;
  min-height: 100vh;

  display: flex;
  justify-content: center;
  align-items: center;
}

/* Column container */
.auth-wrapper {
  width: 100%;
  max-width: 380px;

  display: flex;
  flex-direction: column;
  align-items: center;   /* 👈 KEY FIX */
  gap: 16px;
}

/* Make children same width */
.auth-wrapper > * {
  width: 100%;
}


/* Divider */
.divider {
  text-align: center;
  font-size: 13px;
  color: #888;
  position: relative;
}

.divider::before,
.divider::after {
  content: "";
  position: absolute;
  top: 50%;
  width: 40%;
  height: 1px;
  background: #ddd;
}

.divider::before {
  left: 0;
}

.divider::after {
  right: 0;
}

/* Google button */
.google-login-btn {
  width: 100%;
  height: 44px;

  display: flex;
  align-items: center;
  justify-content: center;   /* 👈 centers content like Login button */
  gap: 8px;                  /* 👈 smaller gap */

  background: #ffffff;
  border: 1px solid #ccc;
  border-radius: 6px;        /* 👈 match login button radius */

  padding: 0;                /* 👈 REMOVE inner padding */
  font-size: 14px;
  font-weight: 500;
  color: #3c4043;
  text-decoration: none;
  cursor: pointer;

  transition: background-color 0.2s, box-shadow 0.2s;
}

.google-login-btn:hover {
  background-color: #f7f8f8;
  box-shadow: 0 1px 2px rgba(60,64,67,0.3);
}

  </style>
</head>

<body>

  <% if (success_msg && success_msg.length > 0) { %>
    <div class="flash-msg success"><%= success_msg %></div>
  <% } %>

  <% if (error_msg && error_msg.length > 0) { %>
    <div class="flash-msg error"><%= error_msg %></div>
  <% } %>

  <!-- PAGE CENTER -->
  <div class="page-center">

    <!-- AUTH COLUMN -->
    <div class="auth-wrapper">

      <!-- LOGIN CARD -->
      <div class="card">
        <h2>Welcome Back</h2>

        <form action="/login" method="POST">
          <label>Email Address</label>
          <input type="email" name="email" required>

          <label>Password</label>
          <input type="password" name="password" required>

          <button type="submit">Login</button>
        </form>

        <div class="links">
          <a href="/register">Register</a> |
          <a href="/forget-password">Forgot Password?</a>
        </div>
      </div>
        <div class="divider">OR</div>

      <!-- GOOGLE BUTTON (OUTSIDE CARD) -->
      <a href="/google" class="google-login-btn">
        <span class="google-icon">
          <svg width="18" height="18" viewBox="0 0 48 48">
            <path fill="#EA4335" d="M24 9.5c3.54 0 6.7 1.22 9.19 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/>
            <path fill="#4285F4" d="M46.5 24c0-1.64-.15-3.21-.43-4.73H24v9.04h12.7c-.55 2.96-2.19 5.47-4.66 7.15l7.53 5.84C43.98 37.18 46.5 31.2 46.5 24z"/>
            <path fill="#FBBC05" d="M10.53 28.41a14.5 14.5 0 0 1 0-8.82l-7.98-6.19a23.97 23.97 0 0 0 0 21.2l7.98-6.19z"/>
            <path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.9-5.81l-7.53-5.84c-2.09 1.41-4.78 2.24-8.37 2.24-6.26 0-11.57-4.22-13.46-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/>
          </svg>
        </span>
        <span>Sign in with Google</span>
      </a>

    </div>
  </div>

</body>
</html>
